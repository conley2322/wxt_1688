# 更新日志

所有版本的更新记录都写在这里。发新版本时：更新 `wxt/package.json` 的 `version` → 在本文件顶部加一节 → 提交并打 `vx.y.z` 标签推送 → 在 GitHub 创建同名 Release。

## v0.2.1（2026-09-26，standalone-local 分支）

### 前端

1. 新增 box2 组件：商品卡片上显示「同供应商已看 N 个商品」，始终可见，无记录显示 0
2. 列表页 / 店铺页 / 以图搜图页刷出卡片时即从页面 DOM 提取供应商名，建立产品↔供应商映射并落库，无需点进详情页
3. box2 数量按供应商名精确匹配，统计该供应商下被浏览过的去重商品数

## v0.2.0 单机版（2026-09-25，standalone-local 分支）

单机版本地化改造：数据全部保存在本机浏览器（IndexedDB），装上即用，不需要任何后端服务器。

### 前端

1. 数据存储改为 IndexedDB，显示存储用量，支持自动清理和备份导出/导入
2. 不再需要登录，装好即用；昵称 + 通用头像可在弹窗里修改
3. box 组件显示两个独立计数：出现次数（页面刷出商品 +1）和浏览次数（点进详情页 +1）
4. box1 组件改为双折线图：最近 14 天的出现/浏览次数趋势
5. 商品卡片不再显示他人数据和评论，只显示自己的统计
6. 管理后台免登录直达，用户管理模块移除，新增存储管理（用量/上限/清理/迁移）

### 后端

1. 移除服务器 —— 数据中枢改为扩展 background，持有扩展 origin 的唯一 IndexedDB
2. 页面脚本通过 runtime 消息读写数据，解决 IndexedDB 按网站隔离导致的数据不互通问题

## v0.1.0（2026-09-06）

### 前端

1. 供应商管理页面加了分页
2. 供应商店铺页面（店铺首页 + 全部商品页）现在也能显示插件了
3. 新增 box1 组件：渲染商品的查询次数和最后一次查询时间
4. 修复商品卡片上重复出现多个插件的问题

### 后端

1. 供应商列表接口加了分页
2. 新增商品查询次数统计：每次查询 +1，记录最后查询时间
